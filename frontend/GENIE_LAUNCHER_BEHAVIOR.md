# Genie launcher behavior

The floating Genie logo is visible while the chatbot is closed. After the user presses the logo and the chat window opens, the floating logo is removed from the application view. Closing the chat restores the launcher.
